"""Domain model: entities, value objects, domain events, and errors.

Provider-specific schemas must never live here. Job sources are represented
only through canonical domain types defined from M1 onward.
"""
